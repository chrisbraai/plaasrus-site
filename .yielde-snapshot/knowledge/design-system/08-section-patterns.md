# Section Patterns

*→ Read before each component in Phase 3. Section patterns define the spatial and visual starting point for every build step.*

### 9.1 Hero
bg `--bg` · padding `80px 32px 56px` · max-width 1280. Overline `--accent`. H1: display-l / lh-tight / max 14ch / `text-wrap: balance`. Emphasis word in accent. 2-button row (primary + ghost). 3× typeset stat + 1× copper highlight. 6px accent slab bottom (effect #03 animated).

### 9.2 Services
<!-- YIELDE:START -->
bg `--bg` · 96px vertical. H2 display-m. 3 cards — **middle on `--bg-subtle`** (AI = the tech moment), outer two on `--bg-raised`. Each card: 2px accent top-rule · 460px min-height · mono `01/02/03` in `--fg-quiet`. H3: display-2xs / 1.0 lh. Hover: effect #04.
<!-- YIELDE:END -->

### 9.3 Process
bg `--bg-raised` · 96 vertical. 3 phase cards on `--bg`. Phase number: display / 120px / lh-display-tight / ls-display-tight. <!-- YIELDE:START -->Middle pre-highlighted accent on `/home`. Scroll-linked on `/process` (effect #05).<!-- YIELDE:END -->

### 9.4 About
bg `--bg` · 96 vertical. 4-stat row all typeset tone. 3 feature cards on `--bg-raised` · 220px min-height. Bragging slab: full-width accent · 40px padding · display-s · arrow-up-right.

### 9.5 Pricing
bg `--bg` · 96 vertical. H2 display-xl / lh-display. 3 tiers · 560px min-height. <!-- YIELDE:START -->Standard highlighted with accent fill + newsprint "Most popular" badge (effect #07).<!-- YIELDE:END --> Tier name display-xs. Features: `--t-body-xs` / accent middot. Full-width button at base.

### 9.6 Contact
bg `--bg-raised` · 96 vertical · max-width 900. H2 display-l. 3-step progress bar (effect #09). Error: `--danger` bg surface · `--danger-ink` text · 2px `--danger` left border. <!-- YIELDE:START -->Success: 6px accent slab + Bebas 80 "Received." (effect #10).<!-- YIELDE:END -->

### 9.7 Work
bg `--bg` · 96 vertical. Dense grid. Client name in mono only — no copy per card. Hover: `--fg` at 90% overlay + client URL + year + services tag. No carousels, filters, tabs, or splits. Scroll. Vignette ambient.

### 9.8 Journal
bg `--bg-newsprint` — warm editorial tone. Body: `--font-serif` / 18px / lh-serif / 65ch measure. H1 per article: display-l / `--fg-inverse`. Pullquotes: display-s. No sidebar, related posts, social sharing, or comments. One article, one page.

### 9.9 Footer
<!-- YIELDE:START -->
bg `--bg` · 6px accent top border. 4-column grid. Wordmark: display-xxl / accent. Amber band (effect #18): 40px `--bg-raised` strip — "You pay after you've seen it. Full stop." Copyright row: 11px / `--fg-muted` / `--ls-footer` / uppercase.
<!-- YIELDE:END -->

### 9.10 Header
<!-- YIELDE:START -->
Sticky · 64px · `--bg` · 1px rule bottom. Wordmark "Yielde^" · display / 30px / accent caret. Desktop nav: `11/500/--ls-micro/uppercase`. Active: accent + 1px underline. Right: `inverse` Button "Start a project."
<!-- YIELDE:END -->
